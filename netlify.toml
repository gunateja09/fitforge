[build]
  command = "npm run build"
  publish = "dist"

# Hash-router SPA: every route is index.html#/..., so no rewrites are needed,
# but this fallback keeps deep links safe if routing ever changes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
